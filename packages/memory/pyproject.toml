[project]
name = "liz-memory"
version = "0.1.1"
description = "Persistent memory module for Liz Coder Plus"
requires-python = ">=3.11"
dependencies = [
    "aiosqlite>=0.20.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"