[pytest]
asyncio_mode = auto
testpaths = .
# Note: each test module is responsible for setting up its own sys.path
# via sys.path.insert() before importing. This avoids namespace clashes
# between packages/core/src and apps/backend/src (both expose a `src`
# package when their parent dir is on sys.path).
